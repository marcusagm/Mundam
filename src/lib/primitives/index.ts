/**
 * Internal Primitives for UI Components
 * 
 * These are low-level hooks and utilities that enable consistent behavior
 * across components (like click-outside, focus traps, controllable state).
 */

export { createClickOutside } from "./createClickOutside";
export { createControllableSignal } from "./createControllableSignal";
export { createFocusTrap } from "./createFocusTrap";
export { createId } from "./createId";
